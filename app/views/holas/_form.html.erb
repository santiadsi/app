<%= form_for(@hola) do |f| %>
  <% if @hola.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hola.errors.count, "error") %> prohibited this hola from being saved:</h2>

      <ul>
      <% @hola.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :texto %><br>
    <%= f.text_field :texto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
